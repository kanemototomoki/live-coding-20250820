/**
 * 数値を2倍にする純粋関数
 * @param value - 元の数値
 * @returns 2倍された数値
 */
export const multiplyBy2 = (value: number): number => {
  return value * 2;
};

/**
 * 数値を3倍にする純粋関数
 * @param value - 元の数値
 * @returns 3倍された数値
 */
export const multiplyBy3 = (value: number): number => {
  return value * 3;
};
